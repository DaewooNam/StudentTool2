MFC
----------------
03.16
----------------


자료형 변수 메모리 항당

DoModal->이 값이 꺼저야 뒤에 내용이 실행됨

IDCANCEL=> 프로그램 꺼질시 IDCANCEL return 값을 반환
이릉 이용해 Domodal control


afx -> static
stdafx MFC 공통 header (학생 정보)

-----------------------------------
library
-----------------------------------
head 공개 (변수선언 함수 선언)
cpp를 암호화(중요 내용)



when 밑에 값들이 변화가 있을시 
lib -> exe lib 따로따로 배포
exe 파일이 커짐
dll -> exe dll 묶어서 배포
exe 파일이 커지지 않지만 묶어서 배포


-------------------------------------------
Login 창 만들기
-------------------------------------------
1. buttion oneClikc event
2. 기본 윈도우 hide(주소값 확인)
3. DoModal을 이용 새창 만들기
4. 숨겨진 창에 대해 값을 꺼주기 위해 IDCANCEL 값을 이용
cf) AfxGetMainWnd 현재 메인 윈도우 창


---------------------------------------------
학생 정보 컨트롤 박스 만들기 
---------------------------------------------
1.Dialog 창에서 각 컨트롤 박스 만든후 ID 값 저장


-----------------------------------------------
학생 정보 생성
-----------------------------------------------
1. stdafx.h 값에 구조체 설정
1.1 Data 값 할당 (name, age, sex, subject) 

2. info head 에서 새로운 함수 정의
2.1 m_pWinCtrl 함수 

-----------------------------------------
이름에 대한 정보를 얻기 
------------------------------------------
1. combobox event 확인

2. Selchange event (List 변환이므로)
2.1 curSel을 이용 index 값 확인
2.2 GetLBText을 이용 str 값 반환

3.info head 에서 새로운 함수 정의
3.1 GetStudentName (이값을 이용해 같은 이름에 대한 정보를 얻음) 
3.2 위 함수는 for문을 이용
3.3 for문을 이용하기 위해 iterator을 이용
3.4 iterator 는 노드의 주소값을 받음 & end까지 실행


4. info head 새로운 함수 정의
4.1 UpdateStudentinfo -> 저장된 정보를 컨트롤 박스에 출력
4.2 각 data를 얻기 위해 getcheck, setchek, gettext


------------------------------------------------------------
새로운 dialog 에서 Veiw
--------------------------------------------------------------






















